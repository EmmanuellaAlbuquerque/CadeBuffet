<!DOCTYPE html>
<html>
  <head>
    <title>CadeBuffet</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
  </head>

  <body>
    <header>
      <nav>
        <ul>
          <li>
            <h1><%= link_to 'Cade Buffet?', root_path %></h1>
          </li>

          <li>
            <% if buffet_owner_signed_in? %>
              <%= button_to 'Sair', destroy_buffet_owner_session_path, method: :delete %> |
              <%= current_buffet_owner.email %>
            <% else %>
              <%= link_to 'Faça seu Login como Dono do Buffet', new_buffet_owner_session_path %>
            <% end %>
          </li>
        </ul>
      </nav>
    </header>

    <main>
      <div>
        <p class="notice"><%= notice %></p>
        <p class="alert"><%= alert %></p>        
      </div>
      
      <%= yield %>
    </main>
  </body>
</html>